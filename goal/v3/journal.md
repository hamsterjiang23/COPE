# v3 过程记录

## 2026-09-13 — 用户授权注册多组合探索

- 用户确认核心目标是有效信息传递带来的学生增益，授权注册 v3、多种组合尝试。
- 固定六个组合：三种接口 × 两个教师层位；两端冻结，仅接口训练。学生无需 Prefill 仅在 kv_replace 组要求。
- 使用全新且排除 v1/v2 所有样本的划分，预注册数值解析与格式分离、开发选择规则及独立留出确认。
- 设置六小时累计预算，不保证找到正结果，不自动追加模型、数据集或种子。
- 服务器预检两张 A800 当前空闲，内存充足，数据盘剩余约 16 GiB；新接口固定瓶颈以控制检查点容量，独立 cope-v3 输出。

- 新增 10 项 CPU 功能检查已通过，覆盖固定数值解析、三种接入的梯度、KV 形状/增长、kv_replace 不读取问题 token、学生参数逐元素不变；随后加强接口实际更新断言并复验。

## 22:05 CST — 首次启动检查停止，未开始训练

- run v3-20260913T140512Z-r01，pueue 17。源码 b7ebb8230e115002f45e6581fec01002d7e2807b17592bd86102e4bf323f3743；运行 12.136 秒，在原生 KV 交接 logits 一致性断言处退出，教师编码次数 0，未产生评测结果或训练检查点。
- 独立诊断同一学生/样本：BF16 完整 Prefill 与逐 token 续接最大 logits 差 0.75，平均差 0.0720，但 greedy token 同为 10061；FP32 最大差 2.861e-5，平均差 6.484e-6，同一 greedy token，1e-4 容差验证通过。证据支持数值路径误差导致 BF16 全词表 allclose 过严。
- 将原生 KV 工程检查改为独立 FP32 学生副本，实验学生保持原 BF16 参数和 buffers，不放宽 FP32 断言。新增启动前失败的预算继承入口；不支持中途训练自动恢复。诊断额外保守计入 60 秒，继承失败运行预算；新 run 不更改固定实验配置。

## 22:09 CST — 恢复启动并通过真实探测

- run v3-20260913T140917Z-r02，pueue 18，组 cope-v3。冻结源码 a4f64e09bb0df5a24745d62da2494240b3fae7cce13d4be76e117c846e436666；模型/数据资产重新哈希，与 v2 固定资产一致，源码逐文件哈希通过。配置保持不变。
- 两个 FP32 原生 cache 接续检查通过，最大 logits 差为 2.6703e-5、3.6240e-5，首 greedy token 一致；正式学生保持 BF16 未改动。峰值显存统计包含这项独立 FP32 副本验证，报告时须注明。
- 六候选各两批真实训练梯度探测通过，共 12 批，每批教师恰好一次编码、loss 有限、projector/reader 梯度有效、双骨干冻结检查通过。当前进入校准/开发基线评测，之后每个候选学习检查通过才正式训练。
- 新划分：512 train、32 calibration、64 dev、256 holdout；全部排除 v1/v2 用过的样本。固定启动 token ID=271，所有样本一致。samples、manifest、assets 与 startup-checks 已本地归档。
- 继承 r01 已消耗约 12.136 秒，另计诊断 60 秒；总预算仍六小时。当前启动恢复只适用于训练前失败，不自动恢复已训练任务。
- 已订阅 pueue follow，开启 cope-v3 完成跟踪；正常运行保持安静，完成、失败或需要处理时通知。没有自动扩展到 v4。

## 2026-09-14 01:04 CST — 完成，确认未通过

- pueue 18 成功退出；六候选各 128 步，选中 middle_embedding 后常量/训练错配各 128 步，全部同顺序、1,024 次正式样本曝光。累计预算 10,576.90 秒，未扩大六小时上限。
- 开发选择按固定 score 执行，仅 middle_embedding 为正，但相对推理错配只多答对一题。锁定后完成六项独立留出评测各 256 题。
- 正确/常量/训练错配/推理错配准确率 55.86/41.02/59.38/56.25%；原始学生 21.48% 且 197/256 题解析失败，教师 98.05%。正确组未优于两项错配，最终状态 inconclusive_or_no_gain。
- 原始 manifest/metrics/artifacts/selection 原样导入；CPU 完整性审计通过：源码和资产哈希、20 份逐题预测重算、配对区间、数据排除、donor、8 组训练曝光与 32 个 checkpoint。完整日志、预测、权重留服务器。
- 汇总更新 results、版本索引和仓库目标/代码/路线总览。42 项 CPU 测试通过；当前不启动 v4。评分混杂和 KV 替代组低分只解释为本配方的限制，不外推所有通道不可行。
- 完成证据和完整 pueue 日志路径/哈希归档后，cope-v3 自动跟踪已暂停。补充 verification.json 记录本次验证，不改写原始 manifest/metrics。
