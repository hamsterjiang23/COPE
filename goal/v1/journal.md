# v1 运行与决策记录

## 2026-09-13 — 计划锁定与准备

- 用户确定 Qwen3-14B → 现有 Qwen3-1.7B，单学生满宽及对照，六小时计算预算。
- 首次检查两张 A800 80GB 空闲；开始实施时数据盘剩余约 55GB，需在每次启动前复查。
- 服务器直连 Hugging Face 失败；通过 SSH 远端 SOCKS 转发访问官方源。下载不计入训练/评测预算。
- 建立版本记录和真实模型入口。尚未运行真实训练，不预填研究结论。

## 下载通路与功能测试

- 官方 Hugging Face 转发下载速度较低。Qwen 在 ModelScope 发布的 8 个权重分片，其 SHA256 与固定 Hugging Face revision 完全一致；改为直连该镜像传输权重，最终仍按原官方 SHA256 校验。配置与 tokenizer 仍取固定官方 revision。
- 首轮 CPU 测试 28 项通过、1 项失败：旧 Pareto 测试误将跨学生排序作为断言。修正为验证逐学生 frontier 成员，不改变原排序行为；新增 padding、恢复、数据隔离和预算测试已通过，修正后重跑。
- 服务器独立解释器通过 .pth 只读复用已有依赖目录，不改动原环境；实际包版本写入运行 manifest。

## 工程验证

- 服务器 CPU 功能测试：30 项全部通过，包含 tiny Qwen3 的 padding 等价性与只返回新 token、错配无自配、标签屏蔽、数据隔离、checkpoint 恢复、预算截止和不可覆盖证据。
- Ruff、git diff --check 和启动脚本语法检查通过。
- 修正公共纠错统计：净收益现在包含教师和学生都错而协同答对的样本；保留教师对/学生错条件纠错率，并补充回归测试。
- 教师侧协议 FP32，骨干 BF16；此版实际 CUDA 传输 payload 为每题 65,536 bytes。

## 2026-09-13 15:57 CST — 首次运行

- run ID：`v1-20260913T075729Z-r01`，独立 pueue 组 `cope-v1`，任务 ID `15`。
- 官方固定 revision 的教师权重与数据全部完成校验；现有学生文件 SHA256 保存到 `/root/autodl-tmp/COPE/outputs/preparation/assets-v1.json`。
- 数据划分：1,024 训练、128 留出；本次长度规则排除 0 条。样本 ID 与源行号保存在本 run 的 `samples.json`，完整数据仅在服务器。
- 从冻结源码快照 `edf3df35f4ec1d78e5ec975de9c7989a9cf30e7eb22d11933cc309137b8c19dc` 执行；archive、未提交补丁、逐文件 SHA256 和实际任务 ID 随 manifest 保存。
- 启动前两卡显存均为 0 MiB、GPU 利用率均为 0%，数据盘可用约 28GB。
- 六小时预算从运行初始化开始累计；安装、下载和数据准备不计入。先进行两批真实模型探测与 16 条训练样本学习检查，检查成功后自动从一致初始化开始四组正式训练。

## 2026-09-13 15:58 CST — 真实模型检查通过

- 两批探测 loss 为 2.8763、1.6879；恰好 2 次教师编码，教师未获得梯度。
- 16 条样本训练 32 步后，NLL 从 2.1018 降至 0.3654；随后恢复一致初始化，开始正式对照。该学习检查只验证实现，不作为留出集收益证据。
- 已启用本任务内的完成跟踪（自动化 `cope-v1`，每 10 分钟检查）；正常运行且无可处理变化时保持安静，完成、失败或需要处理时回收证据并通知。总预算不扩展，结束后停用跟踪。
- 下载完成后关闭本次临时 SOCKS 转发；训练采用本地文件离线加载，不依赖该转发或当前 SSH 会话。

## 2026-09-13 16:52 CST — 运行完成；17:02 CST 起归档

- pueue 任务 15 成功退出，四组均完成共同里程碑 32/64/96/128；七项评测全部完成 128 条。总预算使用 3,280.77 秒，未追加训练或评测。
- 正确 latent 为 88/128（68.75%），无 latent 为 82/128（64.06%），常量为 89/128，错配训练为 94/128；正确模型推理时错配为 85/128。
- 正确相对无 latent 的净差异 +4.69pp，95% 配对区间 [-3.13, 12.50]pp；正确相对常量、错配及推理时错配检查均不足以建立输入相关性证据。研究状态登记为“不确定”，门控未通过。
- 原始学生 118/128 条解析失败。抽查 ID `c5be575b625cb0bf88a5fb4cb9ce5bb51c0233ae1e941428417f842b076e0d2a`、`8266537a81f40010825386e870182851b3776f460802be9863edc5b9e3774bdf`、`31613bcdc2bf2baa6865df2d899b4ba2d8398ac2f9d84e9fa4066c5a69c7cd9e`，输出包含正确 boxed 数值但不满足固定解析格式。因此原始正确性四分组带有格式混杂；没有改动评分或事后替换主结果。
- 运行后 CPU 审计确认：四组各曝光 1,024 个不同训练样本、顺序哈希一致；各组 56 个 LoRA B tensor 均从零初始化发生实际更新；训练和评测错配无自配，全部预测 ID 与留出顺序一致。
- 新增 `scripts/audit_v1_run.py` 仅用于已完成输出的完整性核验，不改变实验源码快照或评分。脚本哈希、日志/预测/16 个检查点的 SHA256 追加于 `postprocess/integrity-audit.json`；原 manifest、metrics、artifacts 保持不可覆盖。
- 完整 pueue 日志归档于服务器 run 下 `postprocess/pueue-15.log`；小型终态 JSON 和完整性索引已同步本地。详细结果、成本和 v2 建议见 results.md；不启动 v2。
- 终态证据和报告同步完成后，自动化 `cope-v1` 已暂停；不再继续定时检查。

## 2026-09-13 — 用户纠正后的适用性说明

用户明确要求双模型冻结、只训练中间套娃模块，并同意新建 v2。v1 实际学生 LoRA 适配与仅满宽设置不符合目标方法；保留全部原始证据，限制其解释范围，不据此否定双冻结套娃协议。
