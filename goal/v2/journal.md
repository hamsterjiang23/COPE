# v2 过程记录

## 2026-09-13 — 依据用户纠正建立版本

- 用户明确：教师与学生均冻结，只训练中间套娃模块，输入为教师问题输入的最后一层 hidden，输出供冻结学生推理使用的适配 hidden。用户同意登记为新的 v2。
- 删除新运行入口中的学生 LoRA 加载、优化与梯度要求；新增仅中间模块可训练的检查。旧 v1 配置会在模型加载前被拒绝，历史复现使用已归档源码。
- no_latent 不再进行训练；raw_student 与 no_latent 共用一次冻结学生评测。
- 暂定四档宽度 256/512/1024/2048，采用同一个教师编码和最大 latent 的多前缀 NLL 均值；增加只训满宽再截断对照。上述宽度是实施选择，尚未执行。
- 沿用数据与格式协议以减少同时变化的变量，明确 v1 留出集已见及严格格式混杂；不把本版称为独立确认实验。
- 新增真实 tiny Qwen 功能测试：验证多前缀生成损失仅更新 projector/reader，教师与学生全部参数逐元素不变，且一次教师编码服务多个宽度。
- 本次只建版、改入口及执行 CPU 功能测试，不启动 GPU 训练或自动跟踪。

## 功能验证

- 完整测试 32 项通过。首次测试工作目录漏同步旧 `configs/arms`，导致一个文件存在性检查失败；补齐测试资源后全套通过，未改动该断言。
- 新增测试验证双模型所有参数在优化前后逐元素相同、projector/reader 实际更新、短宽与满宽对应参数有梯度、每次多宽度损失只编码教师一次、无 latent 没有可训练目标或优化器。
- 旧 LoRA 配方在加载模型前被拒绝；新配方显式登记两端 `frozen=true` 和均匀宽度损失权重，相关配置检查单独复测通过。
- Ruff 与 git diff --check 通过；v1 manifest、metrics、artifacts 的 SHA256 与已归档完整性记录一致。
- 自动汇总导出器改为按实际版本命名，并在 run 下独占创建 `results.generated.md`，不覆盖手写研究结论。

## 2026-09-13 — 用户授权启动

- 用户要求“开始实验”，按当前固定 v2 配方执行。教师、学生均冻结，仅训练中间接口；四档宽度等权监督。
- 启动前两张 A800 空闲，数据盘约 22 GiB 可用。预计全流程 2–4 小时，严格六小时上限；先跑实际多宽度探测和学习检查，检查不通过则停止并诊断。
- 复用已固定模型和数据，重新核验资产；生成包含未提交修改的源码归档、patch 和哈希。独立 `cope-v2` 任务组及 run ID，不修改 v1 运行证据。

## 2026-09-13 18:09 CST — 首次运行启动

- run ID：`v2-20260913T100946Z-r01`；pueue 任务 `16`，组 `cope-v2`。
- 冻结源码快照：`fb00d60605c430a50b1ce780cd1f93e439ec217b90c1d7acc33a26aa7a9346d9`；服务器逐文件校验通过，manifest 保存 commit、未提交补丁和归档哈希。
- 重新核验的模型与数据资产哈希和 v1 资产记录一致；加载原始 Qwen3-1.7B safetensors，没有加载 v1 训练检查点或 LoRA。
- v2 数据单独保存为 `/root/autodl-tmp/COPE/data/v2-prepared.json`；与 v1 样本 ID 核对一致，沿用已声明的探索性留出集口径。
- 两批真实多宽度探测完成，loss 为 2.6602、1.7125，每批恰好一次教师编码。双骨干冻结检查通过，只有中间模块参数可训练；继续 16 条样本学习检查。
- 已开启 `cope-v2` 完成跟踪，正常运行保持安静，完成、失败或需要处理时通知；不扩大六小时预算或自动进入 v3。

## 2026-09-13 18:10 CST — 学习检查通过，进入正式训练

- 16 条训练样本、32 步多宽度学习检查：平均 NLL 从 2.05795 降至 0.38284。只反映训练链路能学习，不是留出集收益证据。
- 学习检查期间峰值 allocated bytes 为 GPU 0：30,329,528,832，GPU 1：11,739,773,952，显存有余量。
- 双模型冻结及无参数梯度检查通过；正式训练前恢复接口初始权重。四组按共同里程碑推进，每组最多 128 步。

## 2026-09-13 20:38 CST — 实验完成及终态审计

- pueue 任务 16 成功退出；四组均完成 128 步，19 份实际评测各 128 题。累计训练与评测预算使用 8,928.52 秒（148.81 分钟），未追加种子或启动 v3。
- correct 在 256/512/1024/2048 维的严格准确率为 60.94/60.16/55.47/50.78%；相对 full-only 的前两档提高 13.28/14.84 个百分点，满宽下降 11.72 个百分点。correct 未可靠优于常量或错配，不满足输入相关性门控。
- 冻结学生严格解析失败 118/128；结果含明显格式混杂。保留预先固定的原始评分，不将严格分数增益直接解释为推理收益。
- 终态 manifest、metrics、artifacts 原样归档到本 run 目录。CPU 完整性审计通过：冻结源码与资产哈希、16 份中间模块检查点、训练顺序、样本隔离、错配 ID、19 份预测和全部配对统计均核验；详见 integrity-audit.json。
- 审计发现 read_latent 先跨卡复制满宽再截取：所有宽度实际复制张量均为 65,536 bytes/题。metrics 中按宽度计算的 payload 是逻辑前缀大小；通过 transport-audit.json 追加更正解释，未覆盖原始指标或修改已运行源码。
- 完整结果、局限及后续建议写入 results.md。本次只归档与审计，不追加训练或扩大研究范围。

- 终态证据及报告归档后，`cope-v2` 完成跟踪已确认设为 PAUSED；不再自动检查本次已完成运行。

## 用户再次纠正 — 直接 Decode 目标与 v2 的边界

- 用户明确要求教师 Prefill 后由套娃接口输出学生可直接 Decode 的状态，学生不再对原问题执行 embedding/Prefill。
- 核验 v2 的 HuggingFaceCausalStudentAdapter：对学生 input_ids 执行 embedding，与 latent 前缀拼接后调用 generate，仍执行学生 Prefill。因此 v2 仅验证了连续前缀注入变体，没有验证用户要求的直接 Decode 路径。
- 原始结果和源码保持不变；向 results 和入口追加适用范围更正。完整 Decode 状态接口及生成 token 的后续处理尚未确定，未启动新版本实验。
